
if (typeof define !== 'function') {
  var define = require('amdefine')(module);
}

define(
  [
    'require',
    'underscore'
  ],
  function (
    require,
    _
  ) {
    
    var EventBus = function () {
      
    };

    return EventBus;
});